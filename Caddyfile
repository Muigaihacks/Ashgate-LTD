{
	frankenphp
}

:{$PORT:8080} {
	root * /app/public
	encode zstd br gzip

	# Try to serve static files first, then fall back to PHP
	@notStatic {
		not file
		not path /favicon.ico
	}
	
	# Serve static files
	file_server
	
	# Handle PHP for non-static requests
	php_server
}
